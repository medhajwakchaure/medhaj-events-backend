name: Backend CI

on:
    push:
        branches: [ develop, main, feature/**, bugfix/**, hotfix/**, release/** ]
        paths: [ "**", "!frontend/**" ]
    pull_request:
        branches: [ develop, main ]
        paths: [ "**", "!frontend/**" ]

jobs:
    build-test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: Setup Java
              uses: actions/setup-java@v4
              with:
                  java-version: '17'
                  distribution: 'temurin'
                  cache: 'maven'

            - name: Build & Test
              run: mvn -B verify

            - name: Spotless Check
              run: mvn -q -Dspotless.check
